{
  "name": "common-backend-toolkit",
  "version": "1.1.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "commit": "cz",
    "release": "standard-version",
    "prepublishOnly": "npm run build",
    "minorRelease": "npm run release -- --release-as minor",
    "majorRelease": "npm run release -- --release-as major",
    "patchRelease": "npm run release -- --release-as patch",
    "publishRepo": "npm run build && npm run release && git push --follow-tags origin main && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.6",
    "cz-conventional-changelog": "^3.3.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.1",
    "@types/express": "^4.17.21",
    "cloudinary": "^2.0.2",
    "commitizen": "^4.3.0",
    "express": "^4.18.3",
    "joi": "^17.12.2",
    "jsonwebtoken": "^9.0.2",
    "redis": "^4.6.13",
    "rimraf": "^5.0.5",
    "standard-version": "^9.5.0",
    "twilio": "^4.23.0",
    "util": "^0.12.5"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
